{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "input": {
      "type": "string",
      "minLength": 1,
      "x-tiwater-file-role": "read"
    },
    "italic": {
      "type": "boolean"
    },
    "indentCharactersPerLevel": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    },
    "tocStyleLevels": {
      "type": "object",
      "properties": {},
      "description": "Optional exact mapping from current TOC entry paragraph style IDs to displayed one-based levels. When provided, every current TOC entry style must be mapped and unused styles are rejected.",
      "additionalProperties": { "type": "integer", "minimum": 1, "maximum": 9 }
    },
    "returnContent": {
      "type": "boolean",
      "description": "Return the complete result directly when it fits the published response limit."
    },
    "output": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute path for a new JSON file containing the complete result. Existing files are never overwritten.",
      "x-tiwater-file-role": "write",
      "x-tiwater-file-effect": false
    }
  },
  "required": [
    "input",
    "italic",
    "indentCharactersPerLevel",
    "returnContent"
  ],
  "additionalProperties": false
}
